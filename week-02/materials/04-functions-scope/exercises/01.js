'use strict';

/*
🧪 Function Scope – Exercises

These tasks help you practice how variables behave inside and outside functions.
*/

// 🟢 Exercise 1: Create a function that declares a local variable and logs it
// Try logging the same variable outside the function (should be undefined)

// ✍️ Your code:

// 🟢 Exercise 2: Create a function `greetUser` that takes a name and logs "Hello, [name]"
// Declare a global variable `name = "Sam"`
// Inside the function, declare another `name = "Alex"` and print both inside and outside

// ✍️ Your code:

// 🟢 Exercise 3: Create a function that defines a variable `count = 10`
// Then inside a nested function, log that same `count`

// ✍️ Your code:

// 🟢 Exercise 4: Write a function that sets a variable without `let`, `const`, or `var`
// Call the function, then log the variable outside the function (should be global – bad practice)

// ✍️ Your code:

// 🟢 Bonus Challenge:
// Create a function `multiplyByTwo(number)` that declares a variable inside it called `result`
// Multiply the input by 2 and return it
// Try accessing `result` outside the function (it should fail)

// ✍️ Your code:
